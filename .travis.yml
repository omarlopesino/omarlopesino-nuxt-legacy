language: node_js
node_js:
- '9.2'
branches:
  only:
  - master

script:
- npm run generate:gh-pages

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $github_token
  keep-history: true
  local_dir: dist
  on:
    branch: gh-pages
